# Uses of Types 
- types/ folder is used to store TypeScript types and interfaces that can be reused across the entire application.

# Example
```bash
export interface User {
  id: string;
  name: string;
  email: string;
  role: string;
}
```